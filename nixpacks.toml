# Coolify Nixpacks Configuration for Next.js 14
[providers]
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps || npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
